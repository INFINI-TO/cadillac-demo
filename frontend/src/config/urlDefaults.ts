/**
 * Dev proxy target when VITE_API_URL is unset (local `npm run dev`).
 * Keep in sync with `frontend/src/config/urlDefaults.ts` (DEV_BACKEND_URL_FALLBACK).
 */
export const DEV_BACKEND_URL_FALLBACK = 'http://localhost:8000';
